#REM Use this command line script to copy files to output directory if needed.
#REM For example it can be usefulll for the app which deploy database by itself.
robocopy $(ProjectDir) "$(TargetDir)EasyFlow" /e /purge /xd bin obj
IF %ERRORLEVEL% EQU 1 (
    exit /b 0
) ELSE (
    exit /b %ERRORLEVEL%
)

<Link>EasySqlFlow\MSSQL\%(RecursiveDir)%(Filename)%(Extension)</Link>
			

  <ItemGroup>
    <Content Include="MSSQL\**">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
			<Link>%(RecursiveDir)%(Filename)%(Extension)</Link>
			<PublishState>EasySqlFlow</PublishState>
			<TargetPath>Test1\MSSQL\</TargetPath>
    </Content>
  </ItemGroup>