# Development 

To start local environment for testing purposes do the following:  
  * Open command line
  * go to `src` folder
  * run: `docker-compose up -d`   

It will start mssql, postgres containers which will be used for testing.

To stop local environment:  
  * Open command line
  * go to `src` folder
  * run: `docker-compose down`   

